export default async function Home() {
  // const client = await createClient();
  // const { data, error } = await client.from("athletes").select("*") as { data: Athlete[], error: Error | null };
  // if (error) {
  //   console.error(error);
  //   return <div>Error loading athletes</div>;
  // }
  return (
    <div className="flex flex-col w-full justify-end p-4">
      
    </div >
  );
}
